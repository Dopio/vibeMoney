import pyautogui
from PIL import ImageGrab, Image
import time


def check_region():
    print("🔍 ПРОВЕРКА ОБЛАСТИ СКАНИРОВАНИЯ")
    print("=" * 50)

    # Ваша область из конфига
    region = (2328, 493, 285, 65)  # x, y, width, height
    x, y, w, h = region

    print(f"📐 Область: {region}")
    print(f"📏 Размер: {w}x{h} пикселей")

    input("📷 Убедитесь что предмет с модами виден и нажмите Enter...")

    # Захватываем скриншот
    screenshot = ImageGrab.grab(bbox=(x, y, x + w, y + h))

    # Сохраняем для просмотра
    screenshot.save('region_check.png')
    print("✅ Скриншот сохранен как 'region_check.png'")

    # Показываем размер
    print(f"📊 Размер скриншота: {screenshot.size}")

    # Показываем скриншот
    screenshot.show()

    # Проверяем есть ли вообще текст визуально
    print("\n👀 Проверьте файл 'region_check.png':")
    print("   - Видны ли моды предмета?")
    print("   - Попадает ли текст '+X to Accuracy' в область?")
    print("   - Не слишком ли маленькая область?")


if __name__ == "__main__":
    check_region()
